const sum = (a,b) =>{console.log(a+b)};
sum(1,2);

const pow = (a,b) => (a**b);   //return keyword is not needed if the function body is a single expression
console.log(pow(2,3));

const double = n => n*2;  //brackets are not needed if there is only one parameter
console.log(double(5));

